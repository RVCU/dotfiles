### FINISHING SETUP
1. open vim and run `:PluginInstall`
2. cd to `~/.vim/bundle/coc-vim/` and run `install.sh`
3. open vim and run `:3. open vim and run `:call Install_Coc()`

then you are good to go :)
